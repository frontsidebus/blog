baseURL = 'https://blog.343-guilty-spark.io/'
languageCode = 'en-us'
title = '343 Guilty Spark'
theme = 'spark'

[params]
  description = 'Security engineering, AI systems, and the infrastructure that holds it all together.'
  author = 'Paul Bryant'

[markup]
  [markup.highlight]
    style = 'dracula'
    lineNos = false
    guessSyntax = true
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true

[pagination]
  pagerSize = 10
